{
  "name": "konexio",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.js",
  "config": {
    "zip_config": ".lambda-zip",
    "zip_name": "konexio.zip",
    "function_name": "konexio"
  },
  "scripts": {
    "test": "dotenv jest",
    "build": "lambda-zip %npm_package_config_zip_config% %npm_package_config_zip_name% -a -v",
    "deploy": "aws lambda update-function-code --function-name %npm_package_config_function_name% --zip-file fileb://%npm_package_config_zip_name%",
    "build-deploy": "npm run build; npm run deploy"
  },
  "author": "Johann Hospice",
  "license": "ISC",
  "dependencies": {
    "airtable": "^0.10.0",
    "aws-sdk": "^2.776.0",
    "aws-xray-sdk": "^3.2.0",
    "twilio": "^3.50.0"
  },
  "devDependencies": {
    "dotenv-cli": "^4.0.0",
    "eslint": "^7.11.0",
    "jest": "^26.6.1",
    "lambda-zip": "^0.3.2"
  }
}
